# Semana 4 — Pandas

**Objetivo:** DataFrame API do Pandas com foco em padrões de produção — method chaining, imutabilidade, merge, otimização de memória.

## Arquivos

| Arquivo | Conteúdo |
|---------|----------|
| `ex3_pandas.ipynb` | Exercícios completos com soluções e análise |

## Conceitos cobertos

- `Series` e `DataFrame` — estrutura interna
- `assign()` vs mutação direta — imutabilidade
- `loc`, `iloc`, `query`, boolean mask
- `groupby + agg` com múltiplas funções
- Method chaining com `pipe()`
- `merge()` e `join()` — tipos e performance
- Otimização de memória: `category`, `downcast`

## Referência

Ver seção **PANDAS** em [../../documentos/exercicios_resolvidos.md](../../documentos/exercicios_resolvidos.md)
